[build]
  command = "pnpm install --no-frozen-lockfile && pnpm run build"
  publish = "artifacts/groovy-gym-bus/dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
